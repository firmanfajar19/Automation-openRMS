name: Playwright Tests Prod

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  pull-requests: write
  checks: write

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: PROD_ENV

    env:
      ENV: ${{ secrets.PROD_ENV }}

    steps:
      # =====================================================
      # Checkout Repository
      # =====================================================
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      # =====================================================
      # Setup Node.js
      # =====================================================
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*

      # =====================================================
      # Install Dependencies
      # =====================================================
      - name: Install dependencies
        run: npm ci

      # =====================================================
      # Install Playwright Browsers
      # =====================================================
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # =====================================================
      # Create .env from GitHub Secret
      # =====================================================
      - name: Create .env file
        run: printf '%s' "$ENV" > .env

      # =====================================================
      # Run Changed Tests (PR only)
      # =====================================================
      - name: Run changed tests (Pull Request)
        if: github.event_name == 'pull_request'
        run: |
          npx bddgen
          npx playwright test --only-changed=origin/${{ github.base_ref }}

      # =====================================================
      # Run Full Tests (Push to main)
      # =====================================================
      - name: Run full Playwright tests
        if: github.event_name == 'push'
        run: npm run test

      # =====================================================
      # Install Allure CLI
      # =====================================================
      - name: Install Allure CLI
        if: github.event_name == 'push' || github.event_name == 'pull_request'
        run: npm install -g allure-commandline

      # =====================================================
      # Restore Allure History (for trend)
      # =====================================================
      - name: Restore Allure history
        if: github.event_name == 'push'
        uses: actions/checkout@v5
        with:
          ref: gh-pages
          path: gh-pages
        continue-on-error: true

      - name: Copy history to allure-results
        if: github.event_name == 'push'
        run: |
          mkdir -p allure-results/history
          cp -r gh-pages/history/* allure-results/history/ || true

      # =====================================================
      # Generate Allure Report
      # =====================================================
      - name: Generate Allure report
        if: github.event_name == 'push' || github.event_name == 'pull_request'
        run: npm run allure

      # =====================================================
      # Generate PR Allure Summary
      # =====================================================
      - name: Generate PR Allure summary
        if: github.event_name == 'pull_request'
        run: |
          export ALLURE_REPORT_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          node .github/scripts/allure-summary.js

      # =====================================================
      # Create / Update PR Comment (ANTI-SPAM)
      # =====================================================
      - name: Create or update PR comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const comment = require('./.github/scripts/pr-comment.js');
            await comment({ github, context });

      # =====================================================
      # Deploy Allure Report to GitHub Pages
      # =====================================================
      - name: Deploy Allure to GitHub Pages
        if: github.event_name == 'push'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-report
          publish_branch: gh-pages
          force_orphan: true

      # =====================================================
      # Upload Allure Artifact (Optional)
      # =====================================================
      - name: Upload Allure report artifact
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: allure-report
          path: allure-report
          retention-days: 30

      # =====================================================
      # Send Email Summary
      # =====================================================
      - name: Send Allure Summary Email
        if: github.event_name == 'push'
        uses: ./.github/actions/allure-summary
        with:
          smtp_host: ${{ secrets.SMTP_HOST }}
          smtp_port: ${{ secrets.SMTP_PORT }}
          smtp_user: ${{ secrets.SMTP_USER }}
          smtp_pass: ${{ secrets.SMTP_PASS }}
          email_to: ${{ secrets.EMAIL_TO }}
          repo_owner: ${{ github.repository_owner }}
          repo_name: ${{ github.event.repository.name }}
